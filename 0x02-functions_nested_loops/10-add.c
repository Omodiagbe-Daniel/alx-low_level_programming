#include <stdio.h>
#include "main.h"

/**
 * add - function that adds two integers
 * @n : num1
 * @m : num2
 * int : returns an integer
 */
int add(int n, int m)
{
int r = n + m;
putchar(r);
return (r);
return (0);
}
