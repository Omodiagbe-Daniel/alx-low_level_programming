#include <stdio.h>
#include "main.h"

/**
 * add - adding two integers
 * int - function type
 * @m - num1
 * @n - num2
 * Return 0 (success)
 */
int add(int, int)
{
putchar((n + '0') + (m + '0'));
return (n + m);
}
