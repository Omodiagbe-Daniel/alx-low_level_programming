#include <stdio.h>
#include "main.h"

/**
 *add - adding two integers
 *@m - an integer
 *@n - an integer
 *Return 0 (success)
 */
int add(int, int)
{
putchar((n + '0') + (m + '0'));
return (n + m)
}
