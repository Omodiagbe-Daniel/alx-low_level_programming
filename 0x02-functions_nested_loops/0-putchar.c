#include <stdio.h>
#include "main.h"
/*
 *Description:codes start and ends here
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char n;

	putchar('_');

	for (n == 'p' || n == 'u' || n == 't' || n == 'c')
		putchar(n);
	for (n == 'h' || n == 'a' || n == 'r')
		putchar(n);
	putchar('\n');
	return (0);
}
