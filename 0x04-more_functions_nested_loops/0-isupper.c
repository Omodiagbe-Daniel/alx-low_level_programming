#include "main.h"
#include <std.io>

/**
 * _isupper: Returns 1 for uppercase alphabets and 0 for lowercase alphabets
 * @c : of type int
 * Return : Always 0 (Success)
 */
int _isupper(int c)
{
c = 65;
while (c <= 90)
{
return (1);
else
	return (0);
}
_putchar(10);
c++
}
